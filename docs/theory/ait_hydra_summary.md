# AIT / HYDRA Summary for CIK

CIK uses AIT/HYDRA as an injection governance layer.

The current injection register lives at:

    outputs/injection/injection_register.json

Current injected artifacts:

- CIF v2.1
- RCC v1.3
- AIT/HYDRA v1.0

The register records how theory layers were injected into the executable runtime. Injection records are governance artifacts, not proof of correctness.